[project]
name = "pyddd"
version = "0.1.0"
description = ""
authors = [
    {name = "v.morugin",email = "vamorugin@gmail.com"}
]
readme = "README.md"
requires-python = "<4.0,>=3.9"
dependencies = [
    "pydantic (>=2.10.5,<3.0.0)"
]
packages = [
    { include = "*", from = "src" },
]



[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"

[tool.poetry.group.dev.dependencies]
pytest = "^8.3.4"
pytest-aio = "^1.9.0"

